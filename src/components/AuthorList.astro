---
import type { PaperAuthor, PaperAuthorNote } from '../data/papers';

interface Props {
  authors: PaperAuthor[];
  notes?: PaperAuthorNote[];
}

const { authors, notes = [] } = Astro.props;
---

<div class="author-block">
  <p class="author-list">
    {authors.map((author, index) => (
      <span class="author">
        {author.isSelf ? <strong>{author.name}</strong> : author.name}
        {author.markers.map((marker) => <sup class="author-marker">{marker}</sup>)}
        {index < authors.length - 1 && <span aria-hidden="true">, </span>}
      </span>
    ))}
  </p>

  {notes.length > 0 && (
    <p class="author-notes">
      {notes.map((note, index) => (
        <span class="author-note">
          <sup class="legend-marker">{note.marker}</sup>{note.text}{index < notes.length - 1 && <span aria-hidden="true"> · </span>}
        </span>
      ))}
    </p>
  )}
</div>

<style>
  /* Hallmark · structured author line: names, markers, notes */
  .author-block {
    display: grid;
    gap: var(--space-2xs);
  }

  .author-list {
    max-width: 72ch;
    color: var(--color-text-soft);
    font-size: var(--text-sm);
    line-height: var(--leading-body);
  }

  .author {
    white-space: normal;
  }

  .author-marker,
  .legend-marker {
    position: relative;
    top: -0.15em;
    color: var(--color-accent);
    font-family: var(--font-label);
    font-size: var(--text-xs);
    font-weight: 700;
    line-height: 0;
  }

  .author-marker {
    margin-inline-start: var(--space-3xs);
  }

  .legend-marker {
    margin-inline-end: var(--space-2xs);
  }

  .author-notes {
    color: var(--color-text-muted);
    font-size: var(--text-xs);
    line-height: var(--leading-body);
  }
</style>
